let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "hash"
    , license = Registry.License.BSD-3-Clause
    , repository = Some
        (Registry.Repo.GitHub { owner = "rightfold", repo = "purescript-hash" })
    , devDependencies =
        toMap { console = "^0.1.1", assert = "^0.1.1", eff = "^0.1.2" }
    , dependencies =
        toMap
          { arrays = "^0.4.5"
          , foldable-traversable = "^0.4.2"
          , generics = "^0.7.2"
          , strings = "^0.7.1"
          }
    }
