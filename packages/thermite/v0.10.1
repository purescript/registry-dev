let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "thermite"
    , license = Registry.License.MIT
    , repository = Some
        (Registry.Repo.GitHub { owner = "paf31", repo = "purescript-thermite" })
    , dependencies =
        toMap { dom = "^0.2.6", react = "^0.4.0", unsafe-coerce = "~0.1.0" }
    }
