let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "xhr"
    , license = Registry.License.MIT
    , devDependencies = toMap { mocha = "*", chai = "*" }
    , dependencies =
        toMap { maybe = "*", foldable-traversable = "*", tuples = "*" }
    }
