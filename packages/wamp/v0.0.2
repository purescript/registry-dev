let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "wamp"
    , license = Registry.License.MIT
    , repository = Some
        (Registry.Repo.Git "https://github.com/kkazuo/purescript-wamp.git")
    , devDependencies = toMap { psci-support = "^2.0.0" }
    , dependencies =
        toMap
          { console = "^2.0.0"
          , maybe = "^2.0.1"
          , aff-promise = "^0.4.0"
          , aff = "^2.0.2"
          , prelude = "^2.1.0"
          }
    }
