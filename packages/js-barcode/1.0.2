let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "js-barcode"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "narinari", repo = "purescript-js-barcode" }
        )
    , devDependencies = toMap { console = "^2.0.0", psci-support = "^2.0.0" }
    , dependencies =
        toMap
          { css = "^2.0.0"
          , dom = "^3.0.0"
          , debug = "^2.0.0"
          , foreign = "^3.0.0"
          , functions = "^2.0.0"
          }
    }
