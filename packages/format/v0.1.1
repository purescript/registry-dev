let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "format"
    , license = Registry.License.MIT
    , repository = Some
        (Registry.Repo.GitHub { owner = "sharkdp", repo = "purescript-format" })
    , devDependencies = toMap { test-unit = "^4.1.0" }
    , dependencies =
        toMap { arrays = "^0.4.5", math = "^0.2.0", strings = "^0.7.1" }
    }
