let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "geometry"
    , devDependencies = toMap { mocha = "*", chai = "*", quickcheck = "*" }
    , dependencies = toMap { math = "*" }
    }
