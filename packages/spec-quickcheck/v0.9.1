let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "spec-quickcheck"
    , dependencies =
        toMap
          { aff = "^1.0.0"
          , spec = "^0.8.0"
          , random = "^1.0.0"
          , quickcheck = "^1.0.0"
          }
    }
