let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "halogen-svg"
    , devDependencies = toMap { psci-support = "^3.0.0" }
    , dependencies =
        toMap { halogen = "^2.0.0", dom-indexed = "^3.0.0", strings = "^3.0.0" }
    }
