let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "matrix"
    , license = Registry.License.`Apache-2.0`
    , repository = Some
        (Registry.Repo.GitHub { owner = "jutaro", repo = "purescript-matrix" })
    , devDependencies =
        toMap { console = "*", assert = "*", benchotron = "*", random = "*" }
    , dependencies =
        toMap { proxy = "*", arrays = "*", extensions = "*", vector = "*" }
    }
