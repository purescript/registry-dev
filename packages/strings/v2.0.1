let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "strings"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "purescript", repo = "purescript-strings" }
        )
    , devDependencies =
        toMap { console = "^2.0.0", assert = "^2.0.0", partial = "^1.1.2" }
    , dependencies = toMap { either = "^2.0.0", maybe = "^2.0.0" }
    }
