let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "identity"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "purescript", repo = "purescript-identity" }
        )
    , dependencies = toMap { foldable-traversable = "^1.0.0-rc.1" }
    }
