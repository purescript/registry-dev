let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "spec-mocha"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "owickstrom", repo = "purescript-spec-mocha" }
        )
    , dependencies = toMap { foldable-traversable = "^2.0.0", spec = "^0.11.0" }
    }
