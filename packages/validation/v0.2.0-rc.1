let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "validation"
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "purescript", repo = "purescript-validation" }
        )
    , dependencies = toMap { control = "^0.3.0" }
    }
