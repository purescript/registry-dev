let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "formulate"
    , devDependencies = toMap { integers = "^4.0.0" }
    , dependencies =
        toMap
          { halogen = "^5.0.0"
          , leibniz = "^5.0.0"
          , record = "^2.0.0"
          , functor1 = "^1.0.0"
          }
    }
