let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "node-readable"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "purescript-node", repo = "purescript-node-readable" }
        )
    , devDependencies = toMap { console = "^3.0.0", st = "^3.0.0" }
    , dependencies =
        toMap { arraybuffer-types = "^2.0.0", node-streams = "^3.2.0" }
    }
