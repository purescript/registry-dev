let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "pqueue"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "nullobject", repo = "purescript-pqueue" }
        )
    , devDependencies = toMap { spec = "^0.12.4" }
    , dependencies =
        toMap { newtype = "^1.3.0", sets = "^2.0.1", lists = "^3.4.0" }
    }
