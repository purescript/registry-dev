let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "csv"
    , repository = Some
        (Registry.Repo.GitHub { owner = "robkuz", repo = "purescript-csv" })
    , dependencies =
        toMap
          { maps = "~0.5.4"
          , console = "^0.1.0"
          , arrays = "~0.4.1"
          , parsing = "~0.8.0"
          , test-unit = "~4.1.0"
          , quickcheck = "~0.12.2"
          }
    }
