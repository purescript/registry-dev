let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "var"
    , license = Registry.License.BSD3
    , repository = Some
        (Registry.Repo.GitHub { owner = "zudov", repo = "purescript-var" })
    , devDependencies = toMap { console = "^0.1.1" }
    , dependencies =
        toMap { invariant = "^0.3.0", contravariant = "^0.2.3", eff = "^0.1.2" }
    }
