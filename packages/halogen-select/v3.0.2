let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "halogen-select"
    , license = Registry.License.`Apache-2.0`
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "citizennet", repo = "purescript-halogen-select" }
        )
    , devDependencies = toMap { debug = "^4.0.0" }
    , dependencies = toMap { halogen-renderless = "^0.0.3", halogen = "^4.0.0" }
    }
