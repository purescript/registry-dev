let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "neon"
    , repository = Some
        (Registry.Repo.GitHub { owner = "tfausak", repo = "purescript-neon" })
    , devDependencies = toMap { test-unit = "4.1.0", quickcheck = "0.12.2" }
    , dependencies = toMap { batteries = "0.5.1" }
    }
