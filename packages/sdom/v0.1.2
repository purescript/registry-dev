let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "sdom"
    , license = Registry.License.MIT
    , repository = Some
        (Registry.Repo.GitHub { owner = "paf31", repo = "purescript-sdom" })
    , devDependencies = toMap { psci-support = "^3.0.0" }
    , dependencies =
        toMap
          { behaviors = "^6.3.0"
          , profunctor = "^3.2.0"
          , dom = "^4.13.2"
          , refs = "^3.0.0"
          }
    }
