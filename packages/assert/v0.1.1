let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "assert"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "purescript", repo = "purescript-assert" }
        )
    , dependencies = toMap { eff = "^0.1.0" }
    }
