let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "assert"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "purescript", repo = "purescript-assert" }
        )
    , dependencies = toMap { console = "^3.0.0", eff = "^3.0.0" }
    }
