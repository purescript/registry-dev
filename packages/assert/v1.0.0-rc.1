let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "assert"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "purescript", repo = "purescript-assert" }
        )
    , dependencies = toMap { eff = "^1.0.0-rc.1" }
    }
