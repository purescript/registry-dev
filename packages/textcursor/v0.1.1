let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "textcursor"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.Git
            "https://github.com/MonoidMusician/purescript-textcursor"
        )
    , devDependencies = toMap { psci-support = "^3.0.0", quickcheck = "^4.4.0" }
    , dependencies =
        toMap
          { newtype = "^2.0.0"
          , profunctor-lenses = "^3.2.0"
          , symbols = "^3.0.0"
          , dom = "^4.3.1"
          , prelude = "^3.0.0"
          }
    }
