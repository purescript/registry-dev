let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "exists-eq"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "rgrempel", repo = "purescript-exists-eq" }
        )
    , devDependencies = toMap { test-unit = "^9.0.0" }
    , dependencies =
        toMap
          { prelude = "^1.0.0", unsafe-coerce = "^1.0.0", tuples = "^1.0.0" }
    }
