let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "domparser"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "toastal", repo = "purescript-domparser" }
        )
    , devDependencies =
        toMap
          { console = "^4.1.0"
          , debug = "^4.0.0"
          , assert = "^4.0.0"
          , psci-support = "^4.0.0"
          }
    , dependencies =
        toMap { effect = "^2.0.0", prelude = "^4.1.0", web-dom = "^1.0.0" }
    }
