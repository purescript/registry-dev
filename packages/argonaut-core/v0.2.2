let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "argonaut-core"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "purescript-contrib", repo = "purescript-argonaut-core" }
        )
    , devDependencies = toMap { strongcheck = "^0.14.0" }
    , dependencies =
        toMap { maps = "^0.5.0", enums = "^0.7.0", functions = "^0.1.0" }
    }
