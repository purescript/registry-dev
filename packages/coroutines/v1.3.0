let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "coroutines"
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "paf31", repo = "purescript-coroutines" }
        )
    , dependencies = toMap { profunctor = "^1.0.0", freet = "^1.0.0" }
    }
