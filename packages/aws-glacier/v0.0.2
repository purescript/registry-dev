let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "aws-glacier"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.Git
            "https://github.com/purescript-aws-sdk/purescript-aws-glacier.git"
        )
    , dependencies = toMap { aws-request = "0.0.5" }
    }
