let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "quaternions"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "hdgarrood", repo = "purescript-quaternions" }
        )
    , devDependencies = toMap { quickcheck = "^0.12.2" }
    , dependencies =
        toMap { console = "^0.1.0", math = "^1.0.0", vector = "^0.3.0" }
    }
