let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "formatters"
    , devDependencies =
        toMap
          { console = "^2.0.0"
          , aff = "^2.0.2"
          , debug = "^2.0.0"
          , psci-support = "^2.0.0"
          }
    , dependencies =
        toMap
          { fixed-points = "^2.0.0"
          , parsing = "^3.0.0"
          , prelude = "^2.1.0"
          , datetime = "^2.0.0"
          }
    }
