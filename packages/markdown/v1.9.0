let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "markdown"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "slamdata", repo = "purescript-markdown" }
        )
    , devDependencies = toMap { strongcheck = "^0.12.1" }
    , dependencies =
        toMap
          { parsing = "^0.5.0", validation = "^0.2.0", functions = "^0.1.0" }
    }
