let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "compact"
    , license = Registry.License.BSD-3-Clause
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "rightfold", repo = "purescript-compact" }
        )
    , devDependencies = toMap { assert = "^1.0.0", eff = "^1.0.0" }
    , dependencies = toMap { prelude = "^1.0.1", unsafe-coerce = "^1.0.0" }
    }
