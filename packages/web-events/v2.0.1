let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "web-events"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "purescript-web", repo = "purescript-web-events" }
        )
    , dependencies =
        toMap { enums = "^4.0.0", nullable = "^4.0.0", datetime = "^4.0.0" }
    }
