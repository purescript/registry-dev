let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "uuid"
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "spicydonuts", repo = "purescript-uuid" }
        )
    , devDependencies = toMap { console = "^0.1.0", spec = "~0.7.2" }
    , dependencies = toMap { eff = "~0.1.2" }
    }
