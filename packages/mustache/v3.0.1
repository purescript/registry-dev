let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "mustache"
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "svisser", repo = "purescript-mustache" }
        )
    , dependencies =
        toMap { maps = "^3.5.1", console = "^3.0.0", mustache = "^2.3.0" }
    }
