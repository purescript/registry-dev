let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "mustache"
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "svisser", repo = "purescript-mustache" }
        )
    , dependencies =
        toMap { maps = "^0.4.2", console = "^0.1.0", mustache = "^2.1.3" }
    }
