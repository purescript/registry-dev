let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "mustache"
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "svisser", repo = "purescript-mustache" }
        )
    , dependencies =
        toMap { maps = "^2.1.1", console = "^2.0.0", mustache = "^2.1.3" }
    }
