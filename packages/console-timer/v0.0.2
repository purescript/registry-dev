let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "console-timer"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "matthewleon", repo = "purescript-console-timer" }
        )
    , dependencies =
        toMap { console = "^2.0.0", symbols = "^2.0.0", prelude = "^2.5.0" }
    }
