let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "semigroups"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.Git
            "https://github.com/matthewleon/purescript-semigroups.git"
        )
    , dependencies =
        toMap { newtype = "^2.0.0", control = "^3.3.1", prelude = "^3.1.1" }
    }
