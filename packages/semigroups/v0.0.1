let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "semigroups"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.Git
            "https://github.com/matthewleon/purescript-semigroups.git"
        )
    , devDependencies = toMap { psci-support = "^3.0.0" }
    , dependencies =
        toMap { newtype = "^2.0.0", console = "^3.0.0", prelude = "^3.1.1" }
    }
