let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "monoid"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "purescript", repo = "purescript-bifunctors" }
        )
    , dependencies = toMap { control = "^0.3.0", invariant = "^0.3.0" }
    }
