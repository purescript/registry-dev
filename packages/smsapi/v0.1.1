let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "smsapi"
    , license = Registry.License.MIT
    , repository = Some
        (Registry.Repo.Git "https://github.com/adamczykm/purescript-smsapi.git")
    , devDependencies = toMap { psci-support = "^4.0.0" }
    , dependencies =
        toMap
          { effect = "^2.0.0"
          , console = "^4.1.0"
          , aff = "^5.0.2"
          , prelude = "^4.1.0"
          }
    }
