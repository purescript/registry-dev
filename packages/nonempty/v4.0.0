let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "nonempty"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "purescript", repo = "purescript-nonempty" }
        )
    , devDependencies = toMap { console = "^3.0.0", assert = "^3.0.0" }
    , dependencies =
        toMap { foldable-traversable = "^3.0.0", prelude = "^3.0.0" }
    }
