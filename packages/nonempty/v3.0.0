let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "nonempty"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "purescript", repo = "purescript-nonempty" }
        )
    , devDependencies = toMap { console = "^2.0.0", assert = "^2.0.0" }
    , dependencies = toMap { foldable-traversable = "^2.0.0" }
    }
