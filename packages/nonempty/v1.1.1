let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "nonempty"
    , license = Registry.License.MIT
    , repository = Some
        (Registry.Repo.GitHub { owner = "paf31", repo = "purescript-nonempty" })
    , devDependencies = toMap { console = "^1.0.0", assert = "^1.0.0" }
    , dependencies =
        toMap { foldable-traversable = "^1.0.0", generics = "^1.0.0" }
    }
