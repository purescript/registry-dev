let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "email-validate"
    , license = Registry.License.BSD3
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "cdepillabout", repo = "purescript-email-validate" }
        )
    , devDependencies = toMap { spec = "^0.7" }
    , dependencies =
        toMap
          { string-parsers = "^0.6"
          , parsing = "^0.7"
          , aff = "^0.13.1"
          , generics = "^0.7"
          , transformers = "^0.8.1"
          }
    }
