let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "unique-lists"
    , license = Registry.License.`AGPL-3.0`
    , repository = Some
        ( Registry.Repo.Git
            "https://github.com/colehaus/purescript-unique-lists.git"
        )
    , devDependencies = toMap { psci-support = "^3.0.0" }
    , dependencies =
        toMap { prelude = "^3.2.0", sets = "^3.1.0", lists = "4.12.0" }
    }
