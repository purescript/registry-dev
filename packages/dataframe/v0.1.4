let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "dataframe"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "gabysbrain", repo = "purescript-dataframe" }
        )
    , devDependencies =
        toMap
          { benchotron = "^6.0.0"
          , psci-support = "^3.0.0"
          , quickcheck = "^4.0.0"
          }
    , dependencies =
        toMap
          { maps = "^3.0.0"
          , console = "^3.0.0"
          , prelude = "^3.0.0"
          , transformers = "^3.2.0"
          }
    }
