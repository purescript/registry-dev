let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "choco-pie"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "justinwoo", repo = "purescript-chocopie" }
        )
    , devDependencies = toMap { assert = "^4.0.0" }
    , dependencies =
        toMap
          { event = "^1.0.0"
          , typelevel-prelude = "^3.0.0"
          , prelude = "^4.0.1"
          , record = "^1.0.0"
          }
    }
