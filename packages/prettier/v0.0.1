let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "prettier"
    , license = Registry.License.MIT
    , repository = Some
        (Registry.Repo.Git "https://github.com/gcanti/purescript-prettier.git")
    , devDependencies = toMap { spec = "^1.0.0", psci-support = "^3.0.0" }
    , dependencies = toMap { maybe = "^3.0.0", prelude = "^3.0.0" }
    }
