let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "control"
    , license = Registry.License.MIT
    , repository = Some
        ( Registry.Repo.GitHub
            { owner = "purescript", repo = "purescript-control" }
        )
    , devDependencies = toMap { eff = "^3.1.0" }
    , dependencies = toMap { prelude = "^3.0.0" }
    }
