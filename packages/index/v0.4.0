let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "index"
    , license = Registry.License.MIT
    , repository = Some
        (Registry.Repo.GitHub { owner = "joneshf", repo = "purescript-index" })
    , dependencies =
        toMap
          { maps = "^0.5.0"
          , arrays = "^0.4.2"
          , maybe = "^0.3.4"
          , identity = "^0.4.0"
          , sets = "^0.5.0"
          }
    }
