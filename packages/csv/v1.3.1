let Registry = ../../RegistryV1.dhall

in  Registry.Package::{
    , name = "csv"
    , license = Registry.License.MIT
    , repository = Some
        (Registry.Repo.Git "https://github.com/nwolverson/purescript-csv.git")
    , devDependencies =
        toMap
          { console = "^2.0.0", test-unit = "^10.1.0", quickcheck = "^3.1.1" }
    , dependencies =
        toMap { maps = "^2.1.1", arrays = "^3.2.0", parsing = "^3.2.0" }
    }
